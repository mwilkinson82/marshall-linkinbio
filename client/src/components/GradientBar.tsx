/**
 * GradientBar — Thin animated gradient bar at the very top of the page.
 * Continuously shifts warm copper/gold colors. Signals premium quality.
 */

export function GradientBar() {
  return (
    <div className="fixed top-0 left-0 right-0 z-50 h-[2px] gradient-bar" />
  );
}
